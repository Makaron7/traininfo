# v0.1.2

UIと内部処理の改善を行ったアップデートです。

## 変更内容
- タブ画面の構成と表示ロジックを調整
- `TrainLCD` 表示まわりの実装を改善
- 権限・位置追跡関連フックを更新
- 依存関係と設定ファイル（`package.json` / `package-lock.json` / `app.json`）を更新
- 到着判定しきい値用フックを追加（`hooks/arrival-threshold.ts`）

## 補足
- 本バージョンは機能改善と内部整理を含みます。
- 次版では動作検証と細かなUI調整を継続予定です。
